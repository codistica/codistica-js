import {BGS} from './custom-backgrounds.js';

const parameters = {
    backgrounds: {
        default: 'Light',
        values: [BGS.LIGHT, BGS.DARK, BGS.TWITTER, BGS.FACEBOOK]
    }
};

export {parameters};
