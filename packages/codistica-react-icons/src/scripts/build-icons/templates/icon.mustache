/** @flow */

{{#hasDynamicIDs}}
    import {useGetUniqueID} from '@codistica/react';
{{/hasDynamicIDs}}
import React from 'react';
import {SvgIcon} from '../utils/svg-icon/svg-icon.js';

type DefaultProps = {|
    title: string,
    size: string | number | null,
    color: string | null,
    {{#hasBackgroundColor}}
        backgroundColor: string | null
    {{/hasBackgroundColor}}
|};

type Props = {
    ...DefaultProps
};

function {{componentName}}(props: Props) {
    {{#hasDynamicIDs}}
        const getUniqueID = useGetUniqueID();
    {{/hasDynamicIDs}}
    const {color,{{#hasBackgroundColor}} backgroundColor,{{/hasBackgroundColor}} ...other} = props;
    return (
        <SvgIcon {...other}>
            {{svgo.data}}
        </SvgIcon>
    );
}

{{componentName}}.defaultProps = ({
    title: '{{title}}',
    size: null,
    color: null,
    {{#hasBackgroundColor}}
        backgroundColor: null
    {{/hasBackgroundColor}}
}: DefaultProps);

export { {{componentName}} };
